pride_and_promises
==================

A comparison of synchronous and asynchronous ways of solving problems.

Installation
------------
Clone the repo. Then `npm install`, and `node app.js` to run the main application. Browse to http://localhost:3000/.

In order to run the benchmark tests, you must have wrk installed. Use `brew install wrk` for that, or some other package manager.
